# 项目新旧架构说明

本项目最初作为基础的 Next.js 渲染方案演示创建，后来进行了全面的现代化架构升级。为了方便对比学习，我们在项目中保留了旧版代码，并统一使用 `_old` 后缀进行了标记。

## 什么是带有 `_old` 的文件？

如果你在项目中看到了带有 `_old` 后缀的文件或路由（例如 `src/pages/pages-router_old.tsx` 或 `tailwind.config_old.ts`），这些代表了**旧版的架构或配置方式**。

它们被保留在代码库中，**纯粹是为了作为新版实现的参考和对比**。

---

## 新旧架构对比概览

### 1. 路由与渲染架构 (Next.js)

本项目演示了服务端渲染（SSR）、静态站点生成（SSG）和增量静态再生（ISR）在两种架构下的不同实现：

| 特性         | 旧版架构 (`_old`)                                    | 新版架构 (当前主力)                                                                             |
| :----------- | :--------------------------------------------------- | :---------------------------------------------------------------------------------------------- |
| **目录**     | `src/pages/` (Pages Router)                          | `src/app/` (App Router)                                                                         |
| **核心机制** | 客户端组件为主，依赖页面级特殊函数获取数据。         | **React Server Components (RSC)** 为默认，组件本身可以在服务端运行并获取数据。                  |
| **SSR 实现** | 必须导出 `getServerSideProps` 函数。                 | 直接在组件中使用 `await fetch(..., { cache: 'no-store' })` 或导出 `dynamic = 'force-dynamic'`。 |
| **SSG 实现** | 必须导出 `getStaticProps` 函数。                     | **默认行为**。普通的 `fetch` 请求默认会被缓存 (`force-cache`) 并在构建时静态化。                |
| **ISR 实现** | 在 `getStaticProps` 返回值中添加 `revalidate` 字段。 | 在文件顶部导出 `export const revalidate = 时间`，或在 `fetch` 选项中配置。                      |

👉 **对比学习建议**：
你可以打开 [src/pages/pages-ssr_old.tsx](./src/pages/pages-ssr_old.tsx) 和 [src/app/ssr/page.tsx](./src/app/ssr/page.tsx) 放在一起对比，直观感受 `getServerSideProps` 模式与 Server Component 模式在代码组织上的巨大差异。

### 2. UI 引擎 (Tailwind CSS)

本项目从旧版 Tailwind CSS 升级到了最新的 v4 版本。

| 特性         | 旧版配置 (`_old`)                                      | 新版配置 (v4)                                                  |
| :----------- | :----------------------------------------------------- | :------------------------------------------------------------- |
| **配置文件** | 依赖 `tailwind.config_old.ts` 进行路径扫描和主题配置。 | **零配置 (Zero-config)**。不再需要 JS/TS 配置文件。            |
| **全局引入** | 使用 `@tailwind base;` 等指令。                        | 仅需一行 `@import "tailwindcss";` (见 `src/app/globals.css`)。 |
| **PostCSS**  | 使用 `tailwindcss` 插件。                              | 使用专用的 `@tailwindcss/postcss` 插件。                       |

---

## 总结

当前项目的主入口和所有现代化的代码均位于 `src/app` 目录下。当你开发新功能时，请**直接在 `src/app` 中基于最新的 App Router 规范进行开发**。

那些 `_old` 文件你可以安全地忽略，或者在需要了解 Next.js 历史演进时翻阅。

---

## 🚀 项目运行与验证指南

### 1. 开发环境运行

要在本地启动开发服务器，运行以下命令：

```bash
npm install   # 确保依赖已安装
npm run dev   # 启动开发服务器
```

默认情况下，应用将运行在 `http://localhost:3000`（如果端口被占用，Next.js 会自动尝试下一个可用端口，请留意终端输出）。
开发模式下支持热更新（HMR），你对文件的修改会立即在浏览器中生效。

### 2. 生产环境构建与预览

为了真正体验和验证 SSG（静态站点生成）和 ISR（增量静态再生）的特性，你需要进行**生产环境构建**。在开发模式 (`npm run dev`) 下，为了方便调试，所有的页面实际上都会在每次请求时被动态渲染。

要验证真正的静态化效果，请按顺序执行：

```bash
npm run build   # 执行生产环境构建
```

**观察构建输出**：
在 `npm run build` 执行完毕后，仔细观察终端输出的路由表（Route）。Next.js 会用不同的符号标记页面的渲染策略：

- `○ (Static)` 或 `● (SSG)`：代表页面已被成功预渲染为静态内容（SSG）。
- `ƒ (Dynamic)`：代表该页面是服务端按需动态渲染的（SSR）。
- `(ISR)`：代表该页面不仅是静态的，还配置了增量更新机制。

构建完成后，启动生产环境服务器：

```bash
npm run start   # 启动生产服务器
```

此时再次访问页面，你可以感受到极致的静态页面加载速度，以及尝试多次刷新来测试 ISR 页面在设定的时间后是如何触发后台更新的。
